![5d94eb2a-a1f8-43f0-bba8-33a34130bc08](https://github.com/user-attachments/assets/2f45f054-9b7e-4ee3-a51d-89702584e8e4)


# 📋 ORM Project - Управление Проектами и Задачами

> RESTful веб-приложение для управления проектами и задачами на базе Spring Boot и Hibernate

![Java](https://img.shields.io/badge/Java-17%2B-brightgreen) ![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.x-green) ![Hibernate](https://img.shields.io/badge/Hibernate-6.x-blue) ![PostgreSQL](https://img.shields.io/badge/PostgreSQL-14%2B-336791) ![Maven](https://img.shields.io/badge/Maven-3.8%2B-C71C22)

---

## 📌 Описание

Сервис позволяет создавать проекты, добавлять к ним задачи и назначать ответственных пользователей. Проект разработан с использованием **Spring Boot** и ORM-фреймворка **Hibernate (JPA)** для обеспечения эффективной работы с базой данных.

### ✨ Основные возможности

- 📊 Управление проектами (создание, редактирование, удаление)
- ✅ Управление задачами в рамках проектов
- 👥 Назначение исполнителей на задачи
- 🔐 Разграничение доступа по ролям (Admin, Manager, User)
- 📚 Интерактивная документация API (Swagger/OpenAPI)
- 🧪 Полное покрытие модульными и интеграционными тестами

---

## ⚙️ Требования

Убедитесь, что в вашей системе установлено:

- **☕ JDK 17+** — для компиляции и запуска приложения
- **📦 Maven 3.8+** — для сборки проекта и управления зависимостями
- **🐘 PostgreSQL 14+** — локально установленный сервер СУБД
- **📝 Postman** (опционально) — для ручного тестирования REST API запросов

> **Примечание:** Проект настроен на использование PostgreSQL по умолчанию. Вы можете изменить настройки подключения в файле конфигурации или через переменные окружения.

---

## 🚀 Быстрый старт

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/Zhidkov-Aleksandr/ORMfinProject.git
cd ORMfinProject
```

### 2️⃣ Создание базы данных PostgreSQL

```sql
CREATE DATABASE orm_final_project;
```

### 3️⃣ Настройка конфигурации

Отредактируйте файл `src/main/resources/application.yml` перед запуском приложения:

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orm_final_project
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
```

**Или установите переменные окружения:**

```bash
export SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/orm_final_project
export SPRING_DATASOURCE_USERNAME=postgres
export SPRING_DATASOURCE_PASSWORD=postgres
```

### 4️⃣ Сборка и запуск приложения

**Запуск с Maven:**

```bash
mvn spring-boot:run
```

**Или сборка JAR и запуск:**

```bash
mvn clean package
java -jar target/orm-final-project-0.0.1-SNAPSHOT.jar
```

### 5️⃣ Доступ к приложению

После успешного запуска приложение будет доступно по адресам:

- **Приложение**: [http://localhost:8080](http://localhost:8080)
- **Swagger UI** (документация API): [http://localhost:8080/swagger-ui/index.html](http://localhost:8080/swagger-ui/index.html)
- **OpenAPI спецификация**: [http://localhost:8080/v3/api-docs](http://localhost:8080/v3/api-docs)

---

## 🏗️ Архитектура приложения

Приложение реализовано с использованием **многослойной архитектуры**:

### 📐 Слои архитектуры

```
┌─────────────────────────────────────────┐
│   Слой представления (Controllers)      │
│   REST контроллеры обработки запросов   │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│   Слой бизнес-логики (Services)        │
│   Инкапсуляция основной функциональности│
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│   Слой доступа к данным (Repositories) │
│   CRUD операции, Spring Data JPA        │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│   Слой модели данных (Entities)        │
│   JPA сущности и отношения между ними  │
└─────────────────────────────────────────┘
```

### 🔹 Компоненты архитектуры

| Слой | Пакет | Описание |
|------|-------|---------|
| **Web** | `controller` | REST-контроллеры, обработка HTTP-запросов |
| **Business Logic** | `service` | Бизнес-логика, валидация, транзакции |
| **Data Access** | `repository` | Spring Data JPA, CRUD операции |
| **Model** | `model` / `entity` | JPA сущности, отображение таблиц БД |
| **Config** | `config` | Конфигурационные классы (CORS, Swagger) |
| **Exceptions** | `exception` | Обработка ошибок и исключений |

### 📊 Отношения сущностей

```
User (1:N)
├── Projects (как менеджер)
└── Tasks (как исполнитель)

Project (1:N)
├── Tasks
└── Team Members (участники)

Task (N:1)
├── Project
└── Assignee (User)
```

---

## 💼 Бизнес-логика

### 📋 Основные use-cases

**Управление проектами:**
- Администраторы и менеджеры создают новые проекты с основной информацией
- Проект может иметь список участников и список связанных задач
- Только пользователи с ролью MANAGER или ADMIN имеют право управлять проектами

**Управление задачами:**
- Для каждого проекта менеджер может добавлять задачи
- Задача содержит: заголовок, описание, статус (TODO/IN_PROGRESS/DONE), приоритет
- Назначать исполнителей могут только менеджеры проектов или администраторы
- Обычные пользователи могут просматривать и менять статус своих задач

**Управление пользователями:**
- Система хранит данные о пользователях с именем, email и ролью
- Тестовые пользователи предусмотрены для демонстрации
- ADMIN имеет доступ ко всем эндпоинтам управления пользователями

**Валидация данных:**
- Название проекта и задачи не могут быть пустыми
- Email пользователя должен быть уникальным и корректного формата
- Приоритеты и статусы задач имеют предопределённые значения

**Каскадирование операций:**
- Удаление проекта приводит к удалению или отвязке связанных задач
- Нельзя удалить пользователя, пока он назначен исполнителем открытых задач
- Система выбросит исключение при нарушении этих правил

---

## 📡 REST API

### Пользователи (User)

```
GET    /api/users              — получить список всех пользователей
POST   /api/users              — создать нового пользователя
GET    /api/users/{id}         — получить информацию о пользователе
PUT    /api/users/{id}         — обновить данные пользователя
DELETE /api/users/{id}         — удалить пользователя
```

### Проекты (Project)

```
GET    /api/projects           — получить список проектов
POST   /api/projects           — создать новый проект
GET    /api/projects/{id}      — получить информацию о проекте
PUT    /api/projects/{id}      — обновить проект
DELETE /api/projects/{id}      — удалить проект
```

### Задачи (Task)

```
GET    /api/projects/{projectId}/tasks  — получить список задач проекта
POST   /api/projects/{projectId}/tasks  — создать новую задачу
GET    /api/tasks/{id}                  — получить информацию о задаче
PUT    /api/tasks/{id}                  — обновить задачу
DELETE /api/tasks/{id}                  — удалить задачу
```

> **Примечание:** Все ответы API возвращаются в формате JSON. При ошибках возвращаются понятные сообщения с соответствующими HTTP статусами (400, 403, 404, 500).

---

## 📚 Документация API (Swagger/OpenAPI)

### 🔍 Swagger UI

После запуска приложения откройте в браузере:

**http://localhost:8080/swagger-ui/index.html**

Здесь доступен удобный web-интерфейс для изучения и тестирования API:
- 📋 Список всех эндпоинтов и методов
- 📝 Параметры запросов и схемы данных
- 🧪 Прямое тестирование запросов из браузера
- 📄 Примеры запросов и ответов

### 🔗 OpenAPI спецификация

Получить спецификацию в JSON формате:

```
http://localhost:8080/v3/api-docs
```

Файл спецификации можно использовать для:
- Интеграции с внешними инструментами
- Генерации клиентского кода
- Импорта в Postman

---

## 📮 Postman коллекция

Для удобства ручного тестирования предоставлена коллекция Postman:

1. **Найдите файл** `ORM-FinalProject.postman_collection.json` в корне репозитория
2. **Откройте Postman** и импортируйте файл
3. **Коллекция содержит:**
   - Все основные эндпоинты
   - Примеры тел запросов (body)
   - Предустановленную авторизацию

> **Авторизация:** Установите тестовые логин/пароль в Postman (см. раздел "Аутентификация")

---

## 🧪 Тестирование

### Модульные тесты

Классы сервисов покрыты модульными тестами с использованием **JUnit 5** и **Mockito** для проверки бизнес-логики изолированно.

### Интеграционные тесты

Написаны интеграционные тесты для проверки взаимодействия с БД. Используется встроенная база **H2** (в памяти) для быстрого запуска.

### Запуск тестов

```bash
mvn test
```

Все тесты должны проходить успешно. При ошибках будет предоставлен стек-трейс с описанием причины.

---

## 🛠️ Стек технологий

| Компонент | Версия | Назначение |
|-----------|--------|-----------|
| **Java** | 17+ | Язык программирования |
| **Spring Boot** | 3.x | Фреймворк разработки приложений |
| **Spring Web (MVC)** | 3.x | REST-контроллеры и HTTP-запросы |
| **Spring Data JPA** | 3.x | Упрощённая работа с JPA/Hibernate |
| **Hibernate** | 6.x | ORM-фреймворк, маппинг объектов на БД |
| **PostgreSQL** | 14+ | Реляционная база данных |
| **Lombok** | 1.18+ | Генерация геттеров/сеттеров |
| **Spring Security** | 6.x | Аутентификация и авторизация |
| **Swagger (springdoc-openapi)** | 2.x | Автогенерация документации API |
| **JUnit 5** | 5.x | Фреймворк модульного тестирования |
| **Mockito** | Latest | Мокирование зависимостей в тестах |
| **H2 Database** | Latest | Встроенная СУБД для тестов |
| **Maven** | 3.8+ | Инструмент сборки проекта |

---

## 🔐 Аутентификация и роли

### HTTP Basic Authentication

Безопасность настроена с помощью **Spring Security**. Для каждого запроса клиент должен предоставить заголовок:

```
Authorization: Basic [base64(username:password)]
```

При отсутствии или неверности учетных данных возвращается статус **401 Unauthorized**.

### Тестовые пользователи

| Логин | Пароль | Роль | Описание |
|-------|--------|------|---------|
| `admin@example.com` | `admin123` | ROLE_ADMIN | Администратор, полный доступ |
| `teacher@example.com` | `teach123` | ROLE_MANAGER | Менеджер проектов |
| `student@example.com` | `stud123` | ROLE_USER | Обычный пользователь |

### Разграничение доступа

- **`/api/users/**`** → 🔒 только ROLE_ADMIN
- **`/api/projects/**`, `/api/tasks/**`** → 🔒 ROLE_MANAGER или ROLE_ADMIN
- **Просмотр и изменение своих задач** → 👤 ROLE_USER
- **Swagger UI** → 🌐 публично доступен

---

## 📁 Структура проекта

```
ORMfinProject/
├── src/main/java/com/example/ormproject/
│   ├── model/               # JPA сущности (User, Project, Task)
│   ├── dto/                 # Data Transfer Objects
│   ├── controller/          # REST контроллеры
│   ├── service/             # Бизнес-логика
│   ├── repository/          # Spring Data JPA репозитории
│   ├── config/              # Конфигурационные классы
│   ├── exception/           # Обработка ошибок
│   └── OrmProjectApplication.java
│
├── src/main/resources/
│   ├── application.yml      # Конфигурация приложения
│   └── logback-spring.xml   # Логирование
│
├── src/test/java/          # Модульные и интеграционные тесты
├── pom.xml                 # Конфигурация Maven
├── README.md               # Этот файл
└── ORM-FinalProject.postman_collection.json  # Postman коллекция
```

---

## 📖 Конфигурация приложения

Основные параметры в `src/main/resources/application.yml`:

```yaml
spring:
  application:
    name: orm-final-project
  
  datasource:
    url: jdbc:postgresql://localhost:5432/orm_final_project
    username: postgres
    password: postgres
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.example.ormproject: DEBUG
```
